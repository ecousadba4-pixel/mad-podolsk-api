### DIM таблицы

- `type_of_smeta` → `dim_estimate`
    - `smeta_code` → `estimate_name`
    - `id_smeta` → `estimate_id`
        
- `smeta_section` → `dim_estimate_section`
    - `id_smeta_section` → `estimate_section_id`
    - `id_smeta` → `estimate_id`
    - `smeta_section` → `estimate_section_name`
        
- `type_of_work` → `dim_work_type`
    - `id_type_of_work` → `work_type_id`
    - `type_work` → `work_type_name`
        
- `unit_list` → `dim_unit`
    - `id_unit` → `unit_id`
    - `unit` → `unit_name`
        
- `work_description` → `dim_work_item`
    - `id_description` → `work_item_id`
    - `description` → `work_name`
    - `id_unit` → `unit_id`
    - `id_smeta` → `estimate_id`
    - `id_smeta_section` → `estimate_section_id`
    - `id_type_of_work` → `work_type_id`
        
- `status_of_work` → `dim_work_status`
    - `id_status` → `work_status_id`
    - `status` → `work_status_name`
        
- `section_of_road` → `dim_road_section`
    - `"id_section of road"` → `road_section_id`
    - `section_of_road` → `road_section_name`
    - `category_of_road` → `road_category`
    - `distance` → `length_km`
    - `width_of_road` → `width_m`
    - `area` → `area_m2`
    - `register_number` → `registry_number`
        
- `dates_table` → `dim_date
    - `date` → `date_day`
    - `month_name` → `month_name`
    - `month_number` → `month_number`
    - `year` → `year`
    - `day_of_week` → `day_of_week`
    - `is_weekend` → `is_weekend`
    - `year_month` → `year_month_key`
    - `month_name_short` → `month_name_short`

### FACT таблицы

- `skpdi_report_raw` → `fact_work_skpdi_report`
    - `date_of_work` → `work_date`
    - `time_of_closing` → `close_time`
    - `id_status` → `work_status_id`
    - `id_section_of_road` → `road_section_id`
    - `id_description` → `work_item_id`
    - `volume_done` → `quantity_done`
    - `comments` → `comment_text`
    - `id_plan` → `plan_row_id`
    - `id_done_work` → `work_report_id`
        
- `skpdi_plan_agg` → `fact_work_plan_monthly`
    - `month_start` → `month_start_date`
    - `description` → `work_name`
    - `unit` → `unit`
    - `planned_volume` → `quantity_planned`
    - `source_files` → `source_files`
    - `loaded_at` → `loaded_at`
        
- `fact_pik_amount` → `fact_work_actual_pik`
    - `date_of_work` → `work_date`
    - `smeta_code` → `estimate_name`
    - `smeta_section` → `estimate_section_name`
    - `description` → `work_name`
    - `volume_done` → `quantity_done`
        
- `prices_2025` → `fact_price_2025`
    - `id_price` → `price_id`
    - `id_smeta` → `estimate_id`
    - `id_smeta_section` → `estimate_section_id`
    - `id_description` → `work_item_id`
    - `price` → `unit_price`
    
- `prices_2026_uptomay` → `fact_price_2026_upto_may`
    - `id_price` → `price_id`
    - `id_smeta` → `estimate_id`
    - `id_smeta_section` → `estimate_section_id`
    - `id_description` → `work_item_id`
    - `price` → `unit_price`
    
- `last_loaded` → `etl_load_state`
    - `last_loaded` → `last_loaded_at`
        
- `podolsk_mad_2025_contract_amount` → `contract_amount_2025`
    - `contract_amount` → `contract_amount`
    
- `podolsk_mad_2026_1sthalf_contract_amount` → `contract_amount_2026_h1`
	- `contract_amount` → `contract_amount`