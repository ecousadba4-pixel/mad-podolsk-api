фронт: Vue 3 + Vite + Pinia + Vue Router;
бэк: Python (FastAPI/Flask) на Amvera, Postgres;
деплой: GitHub Actions → твой хостинг (файл .github/workflows/deploy-vite.yml) и Amvera собирает приложение при каждом commit.
